---
name: craft
version: "11.1.0"
description: Generate UI components with design physics
agent: "crafting-components"
agent_path: .claude/skills/crafting-components/SKILL.md
---

# /craft

Generate design-aware components with physics. Invokes the crafting-components skill.

First, invoke the crafting-components skill:

```
skill({ name: 'crafting-components' })
```

Then follow the skill instructions to generate the component.

<user-request>
$ARGUMENTS
</user-request>
