---
name: sigil-showcasing
version: "1.0.0"
description: |
  Launch Next.js preview app to browse captured components.
  Visual playground for exploring taste patterns.
  "See the guild's finest work."

entry_point: false
invoked_by: "/sigil showcase command"
allowed_tools: [Bash, Read, Write, Glob]

exports:
  - name: showcase
    description: "Launch or build the showcase app"
  - name: export
    description: "Generate JSON/MDX from JSDoc"

prerequisites:
  - tool: node
    install: "brew install node"
    required: true
  - tool: pnpm
    install: "npm install -g pnpm"
    required: false
    fallback: "Uses npm"
---
