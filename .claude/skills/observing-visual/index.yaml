name: observing-visual
version: "9.1.0"
description: Visual feedback loop via Claude in Chrome MCP
tags:
  - observe
  - visual
  - feedback
  - mcp
requires:
  - mcp: claude-in-chrome  # Optional but recommended
outputs:
  - .sigil/observations/screenshots/*.png
  - .sigil/observations/feedback/*.yaml
