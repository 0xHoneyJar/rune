---
name: sigil-querying
version: "1.0.0"
description: |
  Semantic search across captured component taste.
  Uses CK for vector search, falls back to grep.
  "Find the patterns that resonate."

entry_point: false
invoked_by: "/sigil query command"
allowed_tools: [Bash, Read, Glob, Grep]

exports:
  - name: query
    description: "Search components by feel, rejection, or inspiration"

prerequisites:
  - tool: ck
    install: "cargo install ck-search"
    required: false
    fallback: "Uses grep pattern matching"
---
