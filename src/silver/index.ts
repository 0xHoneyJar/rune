/**
 * SILVER REGISTRY — PROVEN PATTERNS
 *
 * Components that have proven useful but haven't reached Gold status.
 * Use Silver when Gold doesn't cover your use case.
 *
 * RULES:
 * - Silver can import from Gold and Silver (no Draft)
 * - Components in Silver are stable but not yet canonical
 * - Typically 3-4 occurrences, consistent usage
 * - Candidates for nomination to Gold
 *
 * PROMOTION PATH:
 * Draft → Silver → Gold (via nomination, never auto-promote)
 *
 * AGENT PROTOCOL:
 * - Check Gold first, then Silver
 * - Silver patterns can be used when Gold lacks archetype
 * - Track usage for potential Gold nomination
 *
 * @sigil-zone registry
 * @sigil-physics N/A
 */

// =============================================================================
// CRITICAL ZONE — Candidates for Gold
// =============================================================================

// Example: export { TransactionButton } from '../components/TransactionButton';

// =============================================================================
// IMPORTANT ZONE — Stable utilities
// =============================================================================

// Example: export { ActionCard } from '../components/ActionCard';

// =============================================================================
// CASUAL ZONE — Proven UI patterns
// =============================================================================

// Example: export { IconButton } from '../components/IconButton';
// Example: export { Badge } from '../components/Badge';

// =============================================================================
// TYPES
// =============================================================================

// Example: export type { IconButtonProps } from '../components/IconButton';
